const themeConfig = {
  exclude: [], // Files to exclude. Accepts glob patterns.
  theme: '', // The default theme.
  scoped: [
    'terra-dev-site-test-theme',
    'orion-fusion-theme',
  ], // An array of scoped themes.
};

module.exports = themeConfig;
