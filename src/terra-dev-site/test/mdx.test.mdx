import NpmBadge from 'terra-doc-template/lib/NpmBadge';
import ReadMe from 'terra-action-header/docs/README.md';
import IndexExampleTemplate from 'terra-doc-template/lib/ExampleTemplate';
import MinimizeCustomButtonActionHeader from 'terra-action-header/lib/terra-dev-site/doc/example/MinimizeCustomButtonActionHeader';
import MinimizeCustomButtonActionHeaderSrc from '!raw-loader!terra-action-header/src/terra-dev-site/doc/example/MinimizeCustomButtonActionHeader';
import { name, version } from 'terra-action-header/package.json';

<NpmBadge
  packageName={'bob'}
  packageVersion={'123'}
/>

{/* <Fragment>{name}</Fragment> */}

# Terra Abstract Modal
The abstract modal is a structural component that provides the ability to display portal'd content in a layer above the app. It consists of an overlay and an unstyled absolute positioned div in which content can be placed. The abstract modal is not intended to be consumed directly, but rather wrapped in a higher order component. Higher order components can provide the abstract modal with sizing, visual styles, and content (e.g; header, body, and actionable buttons). The abstract modals maximum size is constrained by the viewport size, so the content placed inside the modal needs to be responsive.

Our recommendation for HOC is the terra-modal-manager, as it provides sizing, responsive behavior, visual styles, and a disclosure stack.

<ReadMe />

```jsx
<Fragment>{MinimizeCustomButtonActionHeaderSrc}</Fragment>
```

```javascript file:../../../config/site/navigation.config.js live
```

```jsx live=true
() => (
  <h3>
    So functional. Much wow!
  </h3>
)
```

<!-- [View component source code](<Fragment>{`https://github.com/cerner/terra-core/tree/master/packages/${name}`}</Fragment>) -->

# Examples
<IndexExampleTemplate
  example={<MinimizeCustomButtonActionHeader/>}
  exampleSrc={MinimizeCustomButtonActionHeaderSrc}
/>